# AI 使用说明

## 一、AI 提供帮助的关键环节

AI 作为辅助工具参与需求分析、方案设计、编码、验证和文档整理；系统边界、关键取舍及最终实现均经过人工确认。

| 序号 | 关键环节 | AI 提供的帮助 |
| --- | --- | --- |
| 1 | 需求澄清与系统边界收敛 | 根据原始需求梳理系统目标、核心流程和非目标，识别“一条消息只投递一个供应商”“只负责稳定消息投送和处理”等关键约束，协助避免将基础业务系统扩展为通用通知编排平台。 |
| 2 | 架构设计与技术取舍分析 | 提出并比较数据库、消息队列、进程内 Channel、Provider Adapter 等候选方案，分析复杂度、可靠性和演进成本，协助形成 PostgreSQL 保存事实、MQ 只传 `event_id`、单表表达投递状态等设计。 |
| 3 | 设计落地与代码实现 | 按人工确认的方案生成和迭代 Go 代码，将数据模型、HTTP API、Provider Registry、消息发布与消费、状态流转和配置契约转化为可运行实现，并根据审查意见持续调整。 |
| 4 | 可靠性与故障场景推演 | 分析数据库提交失败、MQ 发布失败、重复消息、Worker 崩溃、供应商超时、响应丢失和长期不可用等场景，协助设计内部幂等、至少一次投递、租约恢复、补偿扫描、延迟重试、熔断和失败终态。 |
| 5 | 安全与数据保护审查 | 协助识别接口认证、任意目标地址、供应商凭据、日志和外部响应中的安全风险，完善可信 Provider 配置、密钥隔离、TLS 校验、响应大小限制、Header 白名单和敏感字段脱敏等防护措施。 |
| 6 | 测试设计、验证与缺陷修正 | 协助为 API、Store、MQ、Provider、Publisher、Worker、重试和熔断逻辑设计并补充测试，执行和分析测试、静态检查及并发验证结果，根据失败证据定位实现问题并提出修正方案。 |
| 7 | 接口文档与本地运行环境完善 | 协助统一 API 响应契约和错误语义，生成 Swagger/OpenAPI 文档及调试示例，并完善 Dockerfile、Docker Compose、配置示例和启动说明，降低本地验证与联调成本。 |
| 8 | 性能分析与演进方案设计 | 协助制定端到端压测方案，区分 API、数据库、MQ、Worker 和外部供应商等链路瓶颈，并根据实际指标提出独立扩缩容、分布式限流、Outbox、分区以及补偿器独立部署和 Raft 选主等演进方向。 |
| 9 | 文档整理与决策沉淀 | 协助维护 README、设计说明和取舍记录，将方案变化、采纳或未采纳的建议、适用边界及演进触发条件整理为可追踪文档，便于后续审查、维护和交接。 |

## 二、AI 提出但未采纳的建议

| 序号 | AI 建议 | 未采纳原因 | 最终处理方式 |
| --- | --- | --- | --- |
| 1 | 将系统设计为具备路由和编排能力的通用通知投递平台。 | 项目定位是稳定运行的基础业务系统，通用平台能力会扩大系统边界，并增加升级和维护成本。 | 将系统收敛为稳定可靠的消息投递器，只负责消息受理、持久化、投递及结果查询，不承担通用编排。 |
| 2 | 允许一条事件路由或扇出到多个外部供应商。 | 多供应商投递会引入部分成功、聚合状态、跨供应商一致性及补偿处理等额外复杂度，超出当前需求。 | 系统整体支持多个供应商，但每条消息只绑定一个 `provider_code + provider_action`，需要通知多个供应商时由来源系统分别提交消息。 |
| 3 | 将数据模型拆分为 Event、Route、Endpoint、Task、Attempt、Outbox 和 Dead Letter 等多张表。 | 当前业务关系简单，多表会带来状态同步、事务处理、查询和长期维护成本，属于过度设计。 | 使用单张 `notification_event` 表统一表达业务事件、投递任务、重试、租约、处理结果及供应商响应。 |
| 4 | 在消息分发方案中统一依赖外部消息队列或 Redis。 | 轻量部署场景不需要强制引入额外基础设施，但高吞吐和跨进程部署仍然需要消息队列。 | PostgreSQL 作为事实来源；轻量模式使用进程内 Channel，高吞吐或跨进程模式使用 NSQ/RabbitMQ。 |
| 5 | 使用 `message_type`、Schema 和路由配置描述不同业务变化。 | 这些抽象与实际的供应商动作模型存在重复，增加协议字段和动态配置复杂度。 | 删除 `message_type` 和 `schema_version`，由 `provider_code + provider_action` 决定校验规则和适配逻辑。 |
| 6 | 使用通用的 `external_event_id` 或 `request_id` 作为业务幂等标识。 | 通用命名无法清楚表达该 ID 来自业务系统，也不能体现其与来源系统的对应关系。 | 使用 `source_system + source_request_id` 作为唯一内部幂等键。 |
| 7 | 在 MQ Envelope 中携带独立 `message_id`、Payload 及其他上下文。 | `message_id` 与唯一的 `event_id` 重复，同时在数据库和 MQ 中保存完整上下文会形成两份事实并产生一致性风险。 | MQ 消息体只传递 `event_id`，Worker 消费后从 PostgreSQL 读取完整业务数据。 |
| 8 | 默认向外部供应商传递 `Idempotency-Key`，并依赖它保证外部幂等。 | 外部供应商是否支持幂等键不受本系统控制，不能据此承诺外部操作恰好执行一次。 | 系统只保证内部幂等；只有明确成功才 ACK，其余结果统一 requeue，并接受非幂等操作可能重复的风险。 |
| 9 | 只保存系统内部的投递处理结果。 | 仅有内部结果不足以支持业务排查，还需要保留供应商实际返回的信息，同时避免两类信息混在一起。 | 使用 `last_result` 保存内部处理结果，使用 `provider_response` 保存经过脱敏和截断的供应商响应。 |
| 10 | 主要提供面向内部运维的异步投递状态查询。 | 业务调用方更关心自己提交请求的处理结果，并且不应依赖系统内部 `event_id`。 | 提供基于 `source_request_id` 的查询接口，并显式传入 `source_system` 组成完整查询键。 |

## 三、自主作出的关键决策及原因

| 序号 | 关键决策 | 决策原因 |
| --- | --- | --- |
| 1 | 明确系统目标和能力边界，将系统定位为稳定可靠的消息投递器，只负责消息的受理、持久化、投递和结果查询，不扩展为通用通知编排平台。 | 基础业务系统应优先保证稳定性和可维护性。通过收敛系统职责，可以简化业务模型和运行链路，降低开发、测试及运维成本，避免因引入尚无实际需求的路由、编排和复杂抽象而造成过度设计与系统复杂性。 |
| 2 | 基于生产者—消费者模式解耦消息受理与投递处理，并设计分级消息分发方案：轻量部署采用进程内 Channel，高并发、跨进程或大规模部署时可切换至 NSQ、RabbitMQ 等外部消息队列。 | 生产者—消费者模式能够通过异步处理提升系统并发能力，避免外部供应商响应速度直接阻塞业务请求。进程内 Channel 可降低小规模部署的基础设施和运维成本；预留外部 MQ 扩展路径，则能在业务规模增长时支持多实例消费、弹性扩容和更高吞吐，实现从轻量部署到大规模运行的平滑演进。 |
| 3 | 设计统一的 HTTP 接口协议，规范认证方式、请求结构、响应格式、状态查询方式和错误表达。 | 统一协议可以降低业务系统的接入成本，避免不同调用场景形成不一致的接口约定；同时便于服务端统一校验、错误处理和后续接口维护，提高开发协作与联调效率。 |
| 4 | 提供 Dockerfile 和 Docker Compose 容器化构建与本地运行方案，将应用及其依赖环境进行标准化编排。 | 容器化能够减少开发、测试和部署环境之间的差异，使项目可以快速启动和重复验证，降低环境配置成本，并为后续持续集成及部署提供统一基础。 |
| 5 | 使用 Swagger/OpenAPI 描述并展示服务接口，提供可访问的接口文档和在线调试入口。 | Swagger 能使接口定义、参数和返回结果更加直观，方便开发人员快速理解及验证接口，减少人工维护文档造成的不一致，提高前后端联调、问题定位和测试效率。 |
| 6 | 将 Worker 的重试判定与 MQ 的重投执行拆分。Worker 只负责调用 Provider、记录处理结果并返回统一的 requeue 请求，MQ Driver 负责延迟、退避以及 ACK、NACK 或重新发布等具体机制。 | 业务处理与消息中间件机制各自保持单一职责，避免 Worker 依赖某一种 MQ 的 API；重试策略或 MQ 实现发生变化时只需调整对应边界，使代码更容易测试和维护。 |
| 7 | 抽象统一的 Provider Adapter 接口。Provider Registry 按 `provider_code` 选择 Adapter，再由 Adapter 处理对应的 `provider_action`。 | 不同供应商的配置、认证、请求转换和成功判断存在差异。Adapter 将这些差异封装在各自实现中，使 Worker 只依赖统一调用契约；新增供应商时无需修改核心投递流程。 |
| 8 | 抽象统一的 MQ Publisher、Consumer、Delivery 和 requeue 机制，通过配置切换 MQ Driver。 | Memory、NSQ 和 RabbitMQ 的发布确认、消费确认及重投方式不同。统一接口和语义可以屏蔽产品差异，使系统在不改变业务处理逻辑的情况下灵活切换 Driver，并为后续接入其他 MQ 保留清晰扩展点。 |
