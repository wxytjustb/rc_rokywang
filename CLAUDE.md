# 系统开发约束

## 职责单一

- 每个模块、接口和后台任务只承担一个可清晰描述的职责。
- 业务编排、状态持久化、外部协议适配和运行时调度应保持边界清晰，避免互相渗透。
- 新增能力前优先扩展现有明确边界；若必须拆分，拆分依据应是职责变化，而不是代码量。

## 状态明确

- 业务状态必须有明确含义、合法转换、终态和异常态，禁止用模糊布尔值或日志文本代替状态模型。
- 状态变更必须由唯一、可定位的动作触发，并与持久化结果保持一致。
- 接收成功、处理成功、外部调用成功和业务最终成功必须分别表达，不得混为一谈。

## 协议最小

- API、消息和配置只包含当前业务所必需的字段，不预留没有明确用途的抽象层和扩展字段。
- 协议字段使用稳定、具体的业务语义；同一概念在不同边界保持一致命名。
- 协议演进默认保持向后兼容；破坏性变更必须有明确收益、迁移方案和废弃周期。

## 可靠性边界诚实

- 只承诺系统能够实际证明和保障的可靠性，不把“已接收”“已入队”描述为“已送达”或“已生效”。
- 明确超时、重试、幂等、重复处理、顺序、数据丢失和外部依赖失败的处理边界。
- 无法确认外部操作结果时应进入明确的未知或待核查状态，不得假定成功或直接重复执行高风险操作。

## 可长期稳定运行

- 所有常驻进程和循环任务必须具备资源上限、超时、取消、优雅退出和故障恢复能力。
- 错误必须可观察、可定位、可告警；关键状态变化应保留足够的结构化上下文用于追踪。
- 正确性不得依赖单机内存、偶然执行顺序或人工长期值守；重启和短暂依赖故障后应能安全恢复。

## 需要时再演进

- 默认实现满足当前需求的最小稳定系统，不为假设中的规模、租户或通用平台能力提前设计。
- 引入新组件、抽象或分布式机制前，必须说明当前问题、可验证的演进触发条件和新增运维成本。
- 演进应优先采用可回滚、可分阶段验证的方式，并保留现有稳定路径，直到新方案被证明可靠。
